<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quanlyoto_doan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activivty.RegisterVerhicleInfomation"></activity>
        <activity android:name=".Activivty.Services" />
        <activity android:name=".Activivty.ProductActivity" />
        <activity android:name=".Activivty.HomeActivity" />
        <activity android:name=".Activivty.LoginActivity" />
        <activity android:name=".Activivty.RegisterActivity" />
        <activity android:name=".Activivty.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>